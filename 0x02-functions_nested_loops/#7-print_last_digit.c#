include "main.h"

/**
 * print_sign - check if alphabet is in lowercase
 * @n: parameter for checks
 * Return: 0 ;
 */

int print_last_digit(int n)
{
int last_digit;
last_digit = n % 10;
return (last_digit);
}