#include <stdio.h>
#include "main.h"
/**
 * main - prints all possible combinations of two two-digit numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
char a = 'a';
while (a < 'z')
{
_putchar(a);
a++;
}
_putchar('\n');
return (0);
}
